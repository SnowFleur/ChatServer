규칙

- 멤버 변수는 항상 m_ 붙이기
- 생성자 소멸자등 간단한 멤버함수(Get/set)는 inline 및 헤더파일에 작성
- 하나만 존재야 하는 객체는 복사 생성자, 복사 대입연산자 방지
- 함수는 항상 대문자로 시작
- 자료형은 탭 한번 변수 및 함수는 최대한 줄 맞추기


//21.03.15
- Client Sever 프레임 워크 제작
- ScreenManager 에서 Draw되는거 까지 확인 
- 10개 까지 출력을 하는데 꽉차면 넣고 빼는거 확인 요망

//21.03.16
- Client Server 패킷은 오고가는 거 같음
- BroadCasting 이 조금 이상함 2명한테 못 보냄 확인 요망  ->Client Buffer를 Recv가 아닌 Send로 했음
- 한글 출력을 어떻게 할 것인가... UNICDOE VS MULTIBYTE
- 문자열 입출력 여부
- 콘솔창 두개가 필요한게 맞는가??
- 그룹을 어떻게 만들것인가? (임의로?, 클라가 원하는 걸로?)
- IOCP Buf에 값이 없고 WSABUF에 들어간다??? SDK가 달라서 그런가?   ->Vector에 Push 할 때 복사 생성자가 호출되서 주소값이 변경된것 
- 화면 구성은 어떻게 할 것인가?

//21.03.17
- BroadCasting 은 문제 없이 진행되는거 같음
- Group을 만들자
- 유니, 브로드, 멀티 다 문제없이 진행됨 하지만 아직 문자열이 조금 불안정


//21.03.18
- 나갈 때 본인은 문제없지만 기존 유저들에게서 자기가 안 사라짐 (패킷 한번 더 보내야할듯)

------------------------------------------3주차---------------------------------------------
//21.03.24
- 비동기 Accept 작동 확인
- Lock Guard 만들었지만 아직 테스트 안함
- Room, Session, User Class 생성
- Recv 하고 해당 Sesion에 대한 다형성 까지는 구현했지만 아직 BroadCasting을 어떻게 할지 정하지 않음
- 일단 Accpet에 따른 Index 넘겨주기 부터 Send로 테스트 후 진행해야 할 것으로 보임
- 그 후 Object Pool 을 사용하여 New/Delete 방지 (Session, OverlappedEx)


- 일단 ChatServer를 만들어서 이 클래스가 Chat관련 전반적인 역할을 수행할 수 있도록 구성해야함
- 기존 IOCPManager, WorkThread, Session, OverEx, Lock, RingBuffer는  IOCP 라이브러리 느낌으로 구성하고 
- 그 외 (User, Room, ChatServer)등은 Chat에 특화
- User 단에 내려간 패킷을 어떻게 다시 올릴것인가? ChatServer에서 처리할 수 있도록
- PQCS?, 매개변수로 ChatServer를 내린다?, 전역 함수를 사용한다? 
- ChatServer Run()코드에서 실행할려면 WorkThread WaitForSingle도 어떻게 해야할듯
- 패킷도 새로만들걸로 바꿔야함


